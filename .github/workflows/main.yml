# SUPER FIXED-IP WINDOWS RDP (Tailscale Optimized)
# INSECURE: only for private testing

name: Ultimate Fixed-IP RDP

on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'

concurrency:
  group: fixed-ip-${{ github.repository }}
  cancel-in-progress: true

jobs:
  rdp:
    runs-on: windows-latest
    timeout-minutes: 1439

    steps:
      - name: Enable RDP Access + Disable NLA
        shell: pwsh
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
            -Name "fDenyTSConnections" -Value 0 -Force

          # Disable NLA for smooth RDP
          Set-ItemProperty 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
            -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
            -Name "SecurityLayer" -Value 0 -Force

          # Firewall open
          netsh advfirewall firewall delete rule name="RDP-Fixed" > $null 2>&1
          netsh advfirewall firewall add rule name="RDP-Fixed" dir=in action=allow protocol=TCP localport=3389

          Restart-Service TermService -Force

      - name: Create RDP User (Admin)
        shell: pwsh
        run: |
          $pass = ConvertTo-SecureString "Mustakfnp!" -AsPlainText -Force

          if (Get-LocalUser -Name "RDP" -ErrorAction SilentlyContinue) {
            Set-LocalUser -Name "RDP" -Password $pass
          } else {
            New-LocalUser -Name "RDP" -Password $pass -AccountNeverExpires
            Add-LocalGroupMember -Group "Administrators" -Member "RDP"
            Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"
          }

      - name: Install Tailscale
        shell: pwsh
        run: |
          $url = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $file = "$env:TEMP\ts.msi"
          Invoke-WebRequest -Uri $url -OutFile $file
          Start-Process msiexec.exe -ArgumentList "/i `"$file`" /quiet /norestart" -Wait
          Remove-Item $file

      - name: Tailscale Connect (Static Device)
        shell: pwsh
        env:
          AUTHKEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          if (-not $env:AUTHKEY) {
            Write-Error "Missing TAILSCALE_AUTH_KEY"
            exit 1
          }

          # FIXED hostname → FIXED IP
          $HOST = "mustak-pro-fixed"

          # Cleanup old stale device to avoid duplicates
          & "$env:ProgramFiles\Tailscale\tailscale.exe" logout > $null 2>&1

          # Connect with:
          # • Reusable key
          # • MagicDNS support
          # • Exit-node routing allowed (optional)
          # • Auto NAT traversal + STUN
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up `
             --authkey $env:AUTHKEY `
             --hostname $HOST `
             --accept-routes `
             --accept-dns=true `
             --advertise-exit-node=false `
             --reset

          # Fetch assigned IP
          $TS_IP = (& "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4)[0]
          echo "TSIP=$TS_IP" >> $env:GITHUB_ENV

      - name: Port Check
        shell: pwsh
        run: |
          Write-Host "Tailscale Fixed IP: $env:TSIP"

          if (-not (Test-NetConnection -ComputerName $env:TSIP -Port 3389 -InformationLevel Quiet)) {
            Write-Error "RDP port unreachable"
            exit 1
          }

          Write-Host "RDP OK ✔"

      - name: Keep Alive
        shell: pwsh
        run: |
          while ($true) {
            Write-Host "[$(Get-Date -Format o)] RDP ACTIVE — Fixed IP: $env:TSIP"
            Start-Sleep 300
          }
